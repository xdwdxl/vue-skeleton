export default {
  // Toggle for /api/config/events SSE connection
  enableConfigEvents: false,
  // Toggle for /api/notify/sse-ticket request and SSE connection
  enableNotifySse: false,
  enableModuleFederation: false,
}
